var documenterSearchIndex = {"docs":
[{"category":"section","location":"#DBMigrations.jl","page":"DBMigrations.jl","text":"A Julia package for managing database migrations, compatible with any database package supporting the DBInterface.jl interfaces. The migration file format and schema history table are compatible with Flyway.","title":"DBMigrations.jl"},{"category":"function","location":"#DBMigrations.runmigrations","page":"DBMigrations.jl","text":"DBMigrations.runmigrations(conn::DBInterface.Connection, dir::String; silent=false, splitstatements=true, allowoutoforder=false)\n\nUsing an established database connection conn (which should have the appropriate schema already selected), search the directory dir for migration files and apply them to the database. Migration files should be named like V1__baseline.sql, V2__latlong.sql, etc. where they must start with a capital V followed by a number, followed by two underscores, followed by a description of the migration. The number must be unique across all migrations. The description can be anything, but should be descriptive of the migration. The file extension currently must be .sql.\n\nMigration files found in dir will be checked against a special flyway_schema_history table that the DBMigrations.jl package manages in the database connection for tracking which migrations have already been applied. History rows are matched to local files by version; the installed_rank column records application order (Flyway's semantics for the column, so a history table previously managed by Flyway can be picked up). If a migration file is found in dir that has not been applied, it will be applied to the database. If a migration file is found in dir that has already been applied, it will be skipped. If a migration file is found in dir that has been applied but has changed since it was applied, an error will be thrown (migrations should be immutable once applied).\n\nMigration files may contain multiple SQL statements, separated by semicolons. Each statement will be executed in order. If any statement fails, the entire migration will be rolled back and an error will be thrown. If a migration file contains a syntax error, the migration will be rolled back and an error will be thrown. Semicolons inside single-quoted strings, quoted identifiers, -- and /* */ comments, and Postgres dollar-quoted blocks are not treated as statement separators.\n\nSupported keyword arguments:\n\nsilent::Bool=false: suppress informational logging while applying migrations\nsplitstatements::Bool=true: split each migration file on semicolons and execute each statement separately; pass false to pass each file's contents to the database driver as-is, e.g. for constructs the splitter doesn't understand such as SQLite CREATE TRIGGER bodies with embedded semicolons (note some drivers, e.g. SQLite, only execute the first statement of a multi-statement string, so such constructs should live in their own file)\nallowoutoforder::Bool=false: by default, an OutOfOrderMigrationError is thrown if a pending migration has a version lower than the highest already-applied version (e.g. V2 shows up after V3 was already applied); pass true to apply such migrations anyway\n\nDuplicate migration versions (in the local directory or in the database history table) throw a DuplicateMigrationError. A migration recorded as failed in the history table (possible when the table is shared with other tools like Flyway; this package rolls failed migrations back without recording them) throws a FailedMigrationError and requires manual repair.\n\n\n\n\n\n","title":"DBMigrations.runmigrations"},{"category":"function","location":"#DBMigrations.splitsqlstatements","page":"DBMigrations.jl","text":"DBMigrations.splitsqlstatements(sql::AbstractString)\n\nSplit sql into individual statements on semicolons, ignoring semicolons that appear inside single-quoted strings, double-quoted, backtick-quoted, or bracket-quoted identifiers, line (--) and block (/* */, nesting allowed) comments, and Postgres dollar-quoted ($tag$) blocks. Chunks containing only whitespace/comments are dropped.\n\nQuotes are escaped by doubling (''), per the SQL standard; non-standard backslash-escaped quotes (e.g. MySQL's default \\') are not recognized — use '' or splitstatements=false for such files.\n\n\n\n\n\n","title":"DBMigrations.splitsqlstatements"}]
}
